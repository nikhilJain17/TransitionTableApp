<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="caufcauf.booleanequationgenerator" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material" >
        <activity
            android:name=".HowManyStates"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TwoFlipFlops"
            android:label="@string/title_activity_two_flip_flops" >
        </activity>
        <activity
            android:name=".kmapMinimizer"
            android:label="@string/title_activity_kmap_minimizer" >
        </activity>
    </application>

</manifest>
